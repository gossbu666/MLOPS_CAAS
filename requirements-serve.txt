# ============================================================
# CAAS — Serving-only requirements (FastAPI container)
# Excludes: tensorflow, mlflow, evidently, jupyter, shap, training libs
# Result: ~300MB image vs ~4GB full requirements
# ============================================================

# Core data
pandas==2.2.2
numpy==1.26.4
python-dotenv==1.0.1

# Model
xgboost==2.1.1
lightgbm==4.6.0
joblib>=1.3
scikit-learn==1.5.1

# API serving
fastapi==0.112.0
uvicorn[standard]==0.30.6
pydantic>=2.0

# Dashboard (Streamlit service in docker-compose)
streamlit==1.37.1
altair>=5
requests>=2.31

# AWS (for loading models from S3 in production)
boto3==1.35.0
